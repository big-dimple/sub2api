# LDAP 运维入口

LDAP 版现在只保留一个主运维文档：

- [deploy/README_LDAP_ENTERPRISE.md](../deploy/README_LDAP_ENTERPRISE.md)

请不要再参考旧的网页在线更新说明。当前 fork 的标准升级入口只有：

```bash
cd /path/to/sub2api/deploy
bash upgrade_main.sh
```
